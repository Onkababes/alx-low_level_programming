#include "main.h"

/**
 * _islower - print the function that checks for lowercases
 * @c: the lower character to look at
 * Return: 1 if 'c' is lowercase, otherwise 0
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{

	return (1);
	}
	return (0);

}
