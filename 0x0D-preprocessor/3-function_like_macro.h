#ifndef MACROLIKE_H
#define MACROLIKE_H

/**
 * File name: 3-function_like_macro
 * @x: value of a number
 * Description: function that computes the value of number
 */

#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
